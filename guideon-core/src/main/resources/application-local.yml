# Local Development Profile
# 사용법: --spring.profiles.active=local

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

fastapi:
  service:
    url: http://localhost:8000

logging:
  level:
    org.hibernate.SQL: debug
    com.guideon.core: debug
